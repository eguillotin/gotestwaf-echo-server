# Dependencies
node_modules/

# Reports produced by run-waf-tests.sh
reports/

# SSL material — never commit private keys/certs
*.pem
fullchain.pem
privkey.pem

# Logs and OS cruft
*.log
.DS_Store

# TLS certs for local/EC2 HTTPS (never commit private keys)
certs/
